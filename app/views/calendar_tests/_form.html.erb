<%= form_for(@calendar_test) do |f| %>
  <% if @calendar_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendar_test.errors.count, "error") %> prohibited this calendar_test from being saved:</h2>

      <ul>
      <% @calendar_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :index %><br>
    <%= f.text_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
